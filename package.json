{
  "name": "tonnage",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "description": "TONNAGE - a 5-ton steel cylinder rolls down an endless hill and everything in its path explodes.",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview --port 4173",
    "build:single": "vite build --config vite.single.config.js"
  },
  "dependencies": {
    "@dimforge/rapier3d-compat": "^0.20.0",
    "howler": "^2.2.4",
    "three": "^0.185.1"
  },
  "devDependencies": {
    "vite": "^8.2.2"
  }
}
